[project]
name = "codex-run-budget"
version = "0.19.8"
description = "Shared token guardrails and local usage reports for Codex Tasks and subagents"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "EasyVibeCoding" }]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "W"]
