[project]
name = "codex-run-budget"
version = "0.13.1+codex.20260913005143"
description = "Shared run-scoped token governance for Codex sessions and subagents"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "EasyVibeCoding" }]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "W"]
